{
  "name": "TaseDeck Test MCP",
  "description": "Local test server: tools, arguments, stderr logs.",
  "runCommand": "node SERVER_PATH",
  "jsonConfig": {
    "mcpServers": {
      "tasedeck-test": {
        "command": "node",
        "args": ["SERVER_PATH"],
        "env": {
          "TASEDECK_TEST_ENV": "hello-from-tasedeck"
        }
      }
    }
  },
  "configValues": {
    "__runCommands": "{\"activeId\":\"stdio-profile\",\"commands\":[{\"id\":\"stdio-profile\",\"transport\":\"stdio\",\"command\":\"node\",\"args\":[{\"id\":\"arg-server\",\"name\":\"SERVER_PATH\",\"enabled\":true,\"value\":\"\"}]}]}"
  },
  "notes": [
    "Replace SERVER_PATH with absolute path to test_mcp/server.mjs",
    "In Run commands: Command = node, Arguments = one enabled arg with full path to server.mjs",
    "Or set run_command to: node /absolute/path/to/test_mcp/server.mjs",
    "Logs appear on the MCP process stderr (visible when running server from terminal)"
  ]
}
