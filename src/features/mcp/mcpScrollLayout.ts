/** Sticky card header offset inside the MCP list scrollport (flush below search). */
export const MCP_LIST_STICKY_TOP = 0;

/** Sticky server card header stacks above list scroll clip. */
export const MCP_CARD_HEADER_Z_INDEX = 10;

/** Hides list content that scrolls above sticky card headers (single global scroll). */
export const MCP_LIST_SCROLL_CLIP_Z_INDEX = 8;
